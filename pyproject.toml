[project]
name = "axiora-mcp-server"
version = "0.2.0"
description = "MCP server for Japanese financial data — connects AI agents to 4,000+ listed companies via Axiora"
readme = "README.md"
license = "MIT"
requires-python = ">=3.10"
dependencies = [
    "httpx>=0.27.0",
    "mcp[cli]>=1.3.0",
]
